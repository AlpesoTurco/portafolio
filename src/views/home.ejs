<!DOCTYPE html>
<html lang="es" class="h-full bg-slate-950">
<head>
  <meta charset="UTF-8" />
  <title><%= perfil ? perfil.nombre + ' - Portafolio' : 'Mi Portafolio' %></title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Config básica opcional de Tailwind (colores personalizados) -->
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            primary: '#0ea5e9',   // sky-500
            primaryDark: '#0369a1',
            accent: '#22c55e'
          }
        }
      }
    }
  </script>

  <!-- Estilos para animaciones de entrada por scroll -->
  <style>
    html {
      scroll-behavior: smooth;
    }

    .reveal {
      opacity: 0;
      transform: translateY(24px);
      transition: opacity 0.7s ease-out, transform 0.7s ease-out;
    }

    .reveal.is-visible {
      opacity: 1;
      transform: translateY(0);
    }
  </style>
</head>
<body class="min-h-full bg-slate-950 text-slate-100 antialiased">

  <!-- NAVBAR -->
  <nav class="border-b border-slate-800 bg-slate-900/70 backdrop-blur sticky top-0 z-20">
    <div class="max-w-6xl mx-auto px-4 py-3 flex items-center justify-between">
      <div class="flex items-center gap-2">
        <div class="h-9 w-9 rounded-full bg-primary/20 flex items-center justify-center">
          <span class="text-primary font-bold text-lg">
            <%= perfil ? (perfil.nombre[0] || 'P') : 'P' %>
          </span>
        </div>
        <div>
          <p class="font-semibold text-sm sm:text-base">
            <%= perfil ? perfil.nombre : 'Mi Portafolio' %>
          </p>
          <p class="text-xs text-slate-400">
            <%= perfil ? perfil.titulo_profesional : 'Desarrollador de Software' %>
          </p>
        </div>
      </div>

      <!-- Links de navegación a secciones -->
      <div class="hidden sm:flex items-center gap-4 text-xs sm:text-sm">
        <a href="#perfil" class="text-slate-300 hover:text-primary transition">Perfil</a>
        <a href="#proyectos" class="text-slate-300 hover:text-primary transition">Proyectos</a>
        <a href="#actividades" class="text-slate-300 hover:text-primary transition">Actividades</a>
        <a href="#contacto" class="text-slate-300 hover:text-primary transition">Contacto</a>

        <% if (perfil) { %>
          <% if (perfil.github) { %>
            <a href="<%= perfil.github %>" target="_blank"
               class="px-3 py-1 rounded-full border border-slate-700 hover:border-primary hover:text-primary transition">
              GitHub
            </a>
          <% } %>
          <% if (perfil.linkedin) { %>
            <a href="<%= perfil.linkedin %>" target="_blank"
               class="px-3 py-1 rounded-full bg-primary hover:bg-primaryDark text-slate-950 font-medium transition">
              LinkedIn
            </a>
          <% } %>
        <% } %>
      </div>
    </div>
  </nav>

  <main class="max-w-6xl mx-auto px-4 py-10 space-y-24">

    <!-- PERFIL (pantalla 1) -->
    <section id="perfil" class="min-h-screen flex items-center">
  <div class="grid md:grid-cols-[2fr,1fr] gap-10 items-center w-full reveal">

    <!-- LADO IZQUIERDO: Presentación principal -->
    <div class="space-y-5">
      <% if (perfil) { %>
        <!-- Etiqueta superior -->
        <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-slate-900/80 border border-slate-700/80 text-[11px] text-slate-300 mb-1">
          <span class="inline-block h-1.5 w-1.5 rounded-full bg-emerald-400"></span>
          <span>Portafolio profesional</span>
          <span class="text-slate-500">·</span>
          <span>Desarrollo de software</span>
        </div>

        <!-- Nombre + título -->
        <div>
          <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold mb-2 leading-tight">
            <span class="block text-slate-50">
              <%= perfil.nombre %>
            </span>
            <span class="block text-transparent bg-clip-text bg-gradient-to-r from-primary via-sky-400 to-emerald-400 text-2xl sm:text-3xl mt-1">
              <%= perfil.titulo_profesional %>
            </span>
          </h1>
          <p class="text-xs sm:text-sm text-slate-400 max-w-xl">
            Enfocado en crear soluciones funcionales, mantenibles y pensadas para producción,
            combinando buenas prácticas, bases de datos bien diseñadas y experiencia real en proyectos.
          </p>
        </div>

        <!-- Descripción -->
        <div class="rounded-2xl border border-slate-800 bg-slate-900/70 p-4 sm:p-5">
          <p class="text-sm sm:text-base text-slate-200 leading-relaxed">
            <%= perfil.descripcion %>
          </p>

          <!-- Highlights / enfoque -->
          <div class="mt-4 grid sm:grid-cols-3 gap-3 text-[11px] sm:text-xs">
            <div class="flex flex-col gap-1">
              <span class="text-slate-400">Enfoque principal</span>
              <span class="inline-flex items-center gap-1.5 px-2 py-1 rounded-full bg-slate-950/80 border border-slate-700/80 text-slate-100">
                <span class="inline-block h-1.5 w-1.5 rounded-full bg-primary"></span>
                Desarrollo web / backend
              </span>
            </div>
            <div class="flex flex-col gap-1">
              <span class="text-slate-400">Stack favorito</span>
              <span class="inline-flex items-center gap-1.5 px-2 py-1 rounded-full bg-slate-950/80 border border-slate-700/80 text-slate-100">
                Node.js · Express · MySQL
              </span>
            </div>
            <div class="flex flex-col gap-1">
              <span class="text-slate-400">Lo que aporto</span>
              <span class="inline-flex items-center gap-1.5 px-2 py-1 rounded-full bg-slate-950/80 border border-slate-700/80 text-slate-100">
                Código claro, documentación y responsabilidad
              </span>
            </div>
          </div>
        </div>

        <!-- Botones principales -->
        <div class="flex flex-wrap gap-3">
          <% if (perfil.github) { %>
            <a href="<%= perfil.github %>" target="_blank"
               class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-slate-900 border border-slate-700 hover:border-slate-400 hover:bg-slate-800 text-sm text-slate-100 transition">
              <svg class="h-4 w-4" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <path fill-rule="evenodd" clip-rule="evenodd"
                  d="M12 2C6.48 2 2 6.58 2 12.26C2 16.58 4.87 20.18 8.84 21.5C9.34 21.59 9.52 21.29 9.52 21.03C9.52 20.8 9.51 20.12 9.51 19.39C7 19.93 6.35 18.3 6.35 18.3C5.89 17.12 5.2 16.8 5.2 16.8C4.27 16.14 5.27 16.16 5.27 16.16C6.29 16.23 6.82 17.29 6.82 17.29C7.73 18.91 9.26 18.45 9.86 18.2C9.95 17.5 10.23 17.03 10.53 16.78C8.43 16.53 6.22 15.67 6.22 12.37C6.22 11.39 6.56 10.6 7.12 9.98C7.03 9.73 6.73 8.82 7.2 7.56C7.2 7.56 7.95 7.3 9.51 8.4C10.21 8.2 10.96 8.11 11.71 8.11C12.46 8.11 13.21 8.2 13.91 8.4C15.47 7.3 16.22 7.56 16.22 7.56C16.69 8.82 16.39 9.73 16.3 9.98C16.86 10.6 17.2 11.39 17.2 12.37C17.2 15.68 14.98 16.52 12.88 16.77C13.27 17.11 13.61 17.78 13.61 18.82C13.61 20.28 13.59 21.41 13.59 21.69C13.59 21.95 13.77 22.25 14.27 22.16C18.24 20.84 21.11 17.23 21.11 12.92C21.11 6.58 16.63 2 12 2Z" />
              </svg>
              <span>Ver repositorios</span>
            </a>
          <% } %>

          <% if (perfil.linkedin) { %>
            <a href="<%= perfil.linkedin %>" target="_blank"
               class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary hover:bg-primaryDark text-slate-950 text-sm font-semibold transition">
              <svg class="h-4 w-4" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                <path d="M6.5 6.5C6.5 7.6 5.6 8.5 4.5 8.5C3.4 8.5 2.5 7.6 2.5 6.5C2.5 5.4 3.4 4.5 4.5 4.5C5.6 4.5 6.5 5.4 6.5 6.5Z"/>
                <path d="M3 10H6V20H3V10Z"/>
                <path d="M10 10H13V11.5C13.4 10.8 14.4 10 15.9 10C18.4 10 19 11.6 19 13.9V20H16V14.6C16 13.4 15.7 12.6 14.8 12.6C13.7 12.6 13.2 13.3 13.2 14.6V20H10V10Z"/>
              </svg>
              <span>Conectemos en LinkedIn</span>
            </a>
          <% } %>

          <% if (perfil.email) { %>
            <a href="mailto:<%= perfil.email %>"
               class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-slate-700 hover:border-primary/80 hover:bg-slate-900 text-sm transition">
              <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                <path d="M4 5H20C20.55 5 21 5.45 21 6V18C21 18.55 20.55 19 20 19H4C3.45 19 3 18.55 3 18V6C3 5.45 3.45 5 4 5Z" stroke="currentColor" stroke-width="1.4"/>
                <path d="M5 7L12 12L19 7" stroke="currentColor" stroke-width="1.4"/>
              </svg>
              <span>Contactar por correo</span>
            </a>
          <% } %>
        </div>

        <!-- Info rápida: contacto / ubicación / disponibilidad -->
        <div class="grid sm:grid-cols-3 gap-4 text-xs sm:text-sm text-slate-300">
          <div class="space-y-1">
            <p class="text-slate-400 text-[11px] uppercase tracking-[0.18em]">Contacto</p>
            <% if (perfil.email) { %>
              <p class="flex items-center gap-1.5">
                <span class="text-slate-500">📧</span>
                <span class="font-medium text-slate-100 truncate"><%= perfil.email %></span>
              </p>
            <% } %>
            <% if (perfil.telefono) { %>
              <p class="flex items-center gap-1.5">
                <span class="text-slate-500">📱</span>
                <span class="text-slate-200"><%= perfil.telefono %></span>
              </p>
            <% } %>
          </div>

          <div class="space-y-1">
            <p class="text-slate-400 text-[11px] uppercase tracking-[0.18em]">Perfil</p>
            <% if (perfil.ubicacion) { %>
              <p class="flex items-center gap-1.5">
                <span class="text-slate-500">📍</span>
                <span class="text-slate-200"><%= perfil.ubicacion %></span>
              </p>
            <% } %>
            <p class="flex items-center gap-1.5">
              <span class="text-slate-500">🧩</span>
              <span class="text-slate-200">Desarrollador orientado a resultados</span>
            </p>
          </div>

          <div class="space-y-1">
            <p class="text-slate-400 text-[11px] uppercase tracking-[0.18em]">Estado</p>
            <p class="inline-flex items-center gap-1.5 px-2 py-1 rounded-full bg-emerald-500/10 border border-emerald-500/40 text-emerald-200 text-[11px] sm:text-xs">
              <span class="inline-block h-1.5 w-1.5 rounded-full bg-emerald-400"></span>
              Disponible para nuevas oportunidades
            </p>
          </div>
        </div>

      <% } else { %>
        <h1 class="text-3xl sm:text-4xl font-bold mb-4">Mi Portafolio</h1>
        <p class="text-slate-300">Aún no has configurado tu perfil en la base de datos.</p>
      <% } %>
    </div>

    <!-- LADO DERECHO: Tarjeta con foto / snapshot -->
    <div class="md:justify-self-end">
      <div class="relative rounded-3xl border border-slate-800 bg-gradient-to-b from-slate-900 via-slate-950 to-slate-950 p-5 sm:p-6 shadow-[0_18px_60px_rgba(15,23,42,0.9)] flex flex-col items-center gap-4 overflow-hidden">
        <!-- Glow decorativo -->
        <div class="pointer-events-none absolute inset-0 opacity-60">
          <div class="absolute -top-16 right-[-40px] w-40 h-40 rounded-full bg-primary/20 blur-3xl"></div>
          <div class="absolute bottom-[-40px] left-[-40px] w-40 h-40 rounded-full bg-emerald-400/10 blur-3xl"></div>
        </div>

        <!-- Contenido -->
        <div class="relative z-10 flex flex-col items-center gap-3 w-full">
          <div class="h-24 w-24 sm:h-28 sm:w-28 rounded-full overflow-hidden border-2 border-primary shadow-lg shadow-primary/40">
            <% if (perfil && perfil.foto) { %>
              <img src="<%= perfil.foto %>" alt="Foto de perfil"
                   class="h-full w-full object-cover hover:scale-105 transition-transform duration-300">
            <% } else { %>
              <div class="h-full w-full bg-gradient-to-tr from-primary to-accent flex items-center justify-center text-3xl font-bold text-slate-950">
                <%= perfil ? (perfil.nombre[0] || 'P') : 'P' %>
              </div>
            <% } %>
          </div>

          <div class="text-center space-y-1">
            <p class="font-semibold text-sm sm:text-base text-slate-50">
              <%= perfil ? perfil.nombre : 'Desarrollador' %>
            </p>
            <p class="text-xs text-slate-400">
              <%= perfil ? perfil.titulo_profesional : 'Software Developer' %>
            </p>
            <p class="text-[11px] text-slate-500 max-w-xs">
              Enfocado en construir aplicaciones estables, probadas y listas para producción.
            </p>
          </div>

          <!-- Mini resumen técnico -->
          <div class="w-full mt-2 space-y-2 text-[11px] text-slate-300">
            <div class="flex items-center justify-between gap-2">
              <span class="text-slate-400">Stack backend</span>
              <span class="px-2 py-0.5 rounded-full bg-slate-900/80 border border-slate-700/80">
                Node.js · Express · MySQL
              </span>
            </div>
            <div class="flex items-center justify-between gap-2">
              <span class="text-slate-400">Frontend</span>
              <span class="px-2 py-0.5 rounded-full bg-slate-900/80 border border-slate-700/80">
                EJS · Tailwind CSS
              </span>
            </div>
          </div>

          <div class="w-full border-t border-slate-800 pt-3 mt-2 text-[11px] text-slate-400">
            <p class="flex items-center justify-between gap-2">
              <span>Portafolio construido con:</span>
              <span class="text-slate-200">
                Node.js · Express · MySQL · EJS · Tailwind
              </span>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>


    <!-- PROYECTOS (pantalla 2) -->
    <section id="proyectos" class="min-h-screen flex flex-col justify-center">
      <div class="space-y-6 reveal">
        <!-- Encabezado de sección -->
        <div class="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4">
          <div>
            <p class="text-xs uppercase tracking-[0.3em] text-primary mb-2">
              Portfolio
            </p>
            <h2 class="text-2xl sm:text-3xl font-semibold text-slate-50">
              Proyectos destacados
            </h2>
            <p class="text-sm text-slate-400 mt-1 max-w-xl">
              Una selección de proyectos que representan cómo diseño, desarrollo y despliego soluciones
              para problemas reales, usando buenas prácticas y enfoque en el detalle.
            </p>
          </div>

          <% if (proyectos && proyectos.length > 0) { %>
            <div class="flex flex-col items-start sm:items-end gap-1 text-xs">
              <span class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-slate-900/70 border border-slate-700/70">
                <span class="inline-block h-2 w-2 rounded-full bg-emerald-400"></span>
                <span class="text-slate-300">
                  <%= proyectos.length %> proyectos publicados
                </span>
              </span>
              <span class="text-[11px] text-slate-500">
                Código, demos y tecnologías utilizadas
              </span>
            </div>
          <% } %>
        </div>

        <!-- Grid de proyectos -->
        <% if (proyectos && proyectos.length > 0) { %>
          <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
            <% proyectos.forEach((p, i) => { %>
              <article
                class="relative group rounded-2xl border border-slate-800/70 bg-gradient-to-br from-slate-950/90 via-slate-900/80 to-primary/5
                      p-4 sm:p-5 flex flex-col justify-between overflow-hidden
                      transition-transform transition-shadow duration-300 transform reveal
                      hover:-translate-y-1.5 hover:shadow-[0_0_40px_rgba(56,189,248,0.25)] hover:border-primary/70"
                style="transition-delay:<%= i * 80 %>ms">

                <!-- Glow decorativo -->
                <div class="pointer-events-none absolute inset-0 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                  <div class="absolute -inset-16 bg-[radial-gradient(circle_at_top,_rgba(56,189,248,0.25),transparent_55%)]"></div>
                </div>

                <div class="relative z-10 space-y-3">
                  <!-- Etiqueta arriba -->
                  <div class="flex items-center justify-between gap-2">
                    <span class="inline-flex items-center gap-1.5 px-2 py-1 rounded-full bg-slate-900/80 border border-slate-700/70 text-[11px] text-slate-200">
                      <span class="inline-block h-1.5 w-1.5 rounded-full bg-primary"></span>
                      Proyecto
                    </span>
                    <% if (p.fecha) { %>
                      <span class="text-[11px] text-slate-400">
                        <%= new Date(p.fecha).toLocaleDateString('es-MX', { month:'short', year:'numeric' }) %>
                      </span>
                    <% } %>
                  </div>

                  <!-- Título y descripción -->
                  <div>
                    <h3 class="font-semibold text-base sm:text-lg text-slate-50 mb-1.5 group-hover:text-primary">
                      <%= p.titulo %>
                    </h3>
                    <p class="text-xs sm:text-sm text-slate-300 leading-relaxed">
                      <%= p.descripcion %>
                    </p>
                  </div>

                  <!-- Tecnologías -->
                  <% if (p.tecnologias) { %>
                    <div class="flex flex-wrap gap-1.5 mt-2">
                      <% p.tecnologias.split(',').forEach(t => { %>
                        <span class="inline-flex items-center px-2 py-0.5 rounded-full bg-slate-900/90 border border-slate-700/80
                                      text-[11px] text-slate-200">
                          <span class="inline-block h-1.5 w-1.5 rounded-full bg-slate-500 mr-1"></span>
                          <%= t.trim() %>
                        </span>
                      <% }) %>
                    </div>
                  <% } %>
                </div>

                <!-- Footer de acciones -->
                <div class="relative z-10 mt-4 flex flex-wrap gap-2 text-xs">
                  <% if (p.github) { %>
                    <a href="<%= p.github %>" target="_blank"
                      class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-slate-900/80 border border-slate-700/80
                              text-slate-100 hover:border-slate-400 hover:bg-slate-800 transition">
                      <!-- Icono GitHub minimal -->
                      <svg class="h-3.5 w-3.5" viewBox="0 0 24 24" aria-hidden="true" fill="currentColor">
                        <path fill-rule="evenodd" clip-rule="evenodd"
                          d="M12 2C6.48 2 2 6.58 2 12.26C2 16.58 4.87 20.18 8.84 21.5C9.34 21.59 9.52 21.29 9.52 21.03C9.52 20.8 9.51 20.12 9.51 19.39C7 19.93 6.35 18.3 6.35 18.3C5.89 17.12 5.2 16.8 5.2 16.8C4.27 16.14 5.27 16.16 5.27 16.16C6.29 16.23 6.82 17.29 6.82 17.29C7.73 18.91 9.26 18.45 9.86 18.2C9.95 17.5 10.23 17.03 10.53 16.78C8.43 16.53 6.22 15.67 6.22 12.37C6.22 11.39 6.56 10.6 7.12 9.98C7.03 9.73 6.73 8.82 7.2 7.56C7.2 7.56 7.95 7.3 9.51 8.4C10.21 8.2 10.96 8.11 11.71 8.11C12.46 8.11 13.21 8.2 13.91 8.4C15.47 7.3 16.22 7.56 16.22 7.56C16.69 8.82 16.39 9.73 16.3 9.98C16.86 10.6 17.2 11.39 17.2 12.37C17.2 15.68 14.98 16.52 12.88 16.77C13.27 17.11 13.61 17.78 13.61 18.82C13.61 20.28 13.59 21.41 13.59 21.69C13.59 21.95 13.77 22.25 14.27 22.16C18.24 20.84 21.11 17.23 21.11 12.92C21.11 6.58 16.63 2 12 2Z" />
                      </svg>
                      <span>Código</span>
                    </a>
                  <% } %>

                  <% if (p.demo) { %>
                    <a href="<%= p.demo %>" target="_blank"
                      class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-primary/90 hover:bg-primaryDark
                              text-slate-950 font-medium transition">
                      <!-- Icono de link externo -->
                      <svg class="h-3.5 w-3.5" viewBox="0 0 24 24" aria-hidden="true" fill="currentColor">
                        <path d="M14 3H21V10H19V6.41L11.41 14L10 12.59L17.59 5H14V3Z" />
                        <path d="M5 5H11V7H7V17H17V13H19V19C19 20.1 18.1 21 17 21H7C5.9 21 5 20.1 5 19V5Z" />
                      </svg>
                      <span>Ver demo</span>
                    </a>
                  <% } %>
                </div>
              </article>
            <% }) %>
          </div>
        <% } else { %>
          <p class="text-sm text-slate-400 mt-2">
            No hay proyectos registrados aún. Agrega algunos en tu base de datos para mostrarlos aquí.
          </p>
        <% } %>
      </div>
    </section>


    <!-- ACTIVIDADES / EXPERIENCIA (pantalla 3) -->
    

    <!-- HABILIDADES (pantalla 4, opcionalmente puede ir dentro de actividades) -->
    <section id="habilidades" class="min-h-[70vh] flex flex-col justify-center">
      <div class="space-y-3 reveal">
        <h2 class="text-xl sm:text-2xl font-semibold">Habilidades</h2>

        <% if (habilidades && habilidades.length > 0) { %>
          <div class="rounded-2xl border border-slate-800 bg-slate-900/60 p-4">
            <div class="flex flex-wrap gap-1.5 mb-3 text-[11px] text-slate-400">
              <span class="px-2 py-0.5 rounded-full bg-slate-800 border border-slate-700">
                Lenguajes
              </span>
              <span class="px-2 py-0.5 rounded-full bg-slate-800 border border-slate-700">
                Frameworks
              </span>
              <span class="px-2 py-0.5 rounded-full bg-slate-800 border border-slate-700">
                Bases de datos
              </span>
              <span class="px-2 py-0.5 rounded-full bg-slate-800 border border-slate-700">
                Redes / Herramientas
              </span>
            </div>

            <div class="flex flex-wrap gap-2">
              <% habilidades.forEach(h => { %>
                <div class="px-3 py-1.5 rounded-full border border-slate-700 bg-slate-950/40 text-xs flex items-center gap-2">
                  <span class="font-medium text-slate-100"><%= h.nombre %></span>
                  <span class="text-[10px] px-1.5 py-0.5 rounded-full 
                              <%= h.nivel === 'Avanzado' ? 'bg-emerald-500/20 text-emerald-300' 
                                : h.nivel === 'Intermedio' ? 'bg-amber-500/20 text-amber-300'
                                : 'bg-slate-500/20 text-slate-200' %>">
                    <%= h.nivel %>
                  </span>
                  <span class="text-[10px] text-slate-400">
                    <%= h.tipo %>
                  </span>
                </div>
              <% }) %>
            </div>
          </div>
        <% } else { %>
          <p class="text-sm text-slate-400">
            Todavía no has registrado habilidades. Añádelas en tu tabla <code class="text-xs text-primary">habilidades</code>.
          </p>
        <% } %>
      </div>
    </section>

    <!-- CONTACTO (pantalla final) -->
    <section id="contacto" class="border-t border-slate-800 pt-10 mt-4 min-h-[50vh] flex flex-col justify-center">
      <div class="reveal">
        <h2 class="text-xl sm:text-2xl font-semibold mb-3">Contacto</h2>
        <p class="text-sm text-slate-300 mb-3">
          ¿Te interesa lo que hago? Estoy disponible para oportunidades como desarrollador.
        </p>

        <% if (perfil && perfil.email) { %>
          <a href="mailto:<%= perfil.email %>"
             class="inline-flex items-center px-4 py-2 rounded-full bg-primary hover:bg-primaryDark text-slate-950 font-semibold text-sm transition">
            Enviar correo
          </a>
        <% } else { %>
          <p class="text-sm text-slate-400">
            Configura tu correo en la tabla <code class="text-xs text-primary">perfil</code> para habilitar el contacto directo.
          </p>
        <% } %>
      </div>
    </section>

  </main>

  <!-- FOOTER -->
  <footer class="border-t border-slate-900 bg-slate-950/90">
    <div class="max-w-6xl mx-auto px-4 py-4 flex flex-col sm:flex-row items-center justify-between gap-2 text-xs text-slate-500">
      <p>
        © <%= new Date().getFullYear() %> 
        <span class="text-slate-300 font-medium"><%= perfil ? perfil.nombre : 'Desarrollador' %></span>. 
        Todos los derechos reservados.
      </p>
      <p class="text-[11px]">
        Construido con Node.js · Express · MySQL · EJS · Tailwind CSS
      </p>
    </div>
  </footer>

  <!-- JS: IntersectionObserver para animaciones por scroll -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            observer.unobserve(entry.target);
          }
        });
      }, {
        threshold: 0.2
      });

      document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
    });
  </script>
</body>
</html>
